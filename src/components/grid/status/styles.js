import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    dimensions: {
      fontSize: 20,
      color: "gold",
      fontStyle: 'italic'
  
    }
  });
  
 
export default styles;