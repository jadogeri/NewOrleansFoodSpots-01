import { StyleSheet } from "react-native-web";

const styles = StyleSheet.create({
    container:{
       marginBottom:10
    },
    title:{
        fontSize:20,
        fontWeight: 'bold',
        marginLeft :15
    }
});

export default styles;