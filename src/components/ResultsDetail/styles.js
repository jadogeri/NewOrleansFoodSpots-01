import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    image: {
        height: 200,
        width: 250,
        borderRadius: 4,
        marginLeft: 10
    },
    name: {
        fontWeight: "bold",
        fontSize: 20
    }
})

export default styles;